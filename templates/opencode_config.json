{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "cortext": {
      "type": "local",
      "command": ["cortext-mcp"],
      "enabled": true,
      "environment": {
        "WORKSPACE_PATH": "{{WORKSPACE_PATH}}"
      }
    }
  }
}
